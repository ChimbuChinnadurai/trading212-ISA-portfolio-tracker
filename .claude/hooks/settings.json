{
    "hooks": {
        "postToolUse": [
            {
                "matcher": "Edit|Write",
                "command": "python -m py_compile $(echo \"$CLAUDE_TOOL_INPUT\" | jq -r '.file_path // empty' | grep '\\.py$') 2>&1 || true"
            }
        ]
    }
}